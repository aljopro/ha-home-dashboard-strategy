// Test setup for Vitest + jsdom
// Import jest-dom matchers so we can use `toBeInTheDocument()` etc.
import '@testing-library/jest-dom';

// Optionally set up other globals here (e.g., global fetch mocks)
